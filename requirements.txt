# OCR & image processing (ocr-service)
pytesseract==0.3.13
pdf2image==1.17.0
Pillow==10.3.0
opencv-python==4.9.0.80
ultralytics==8.1.0

# NER & NLP (ner-service)
fastapi==0.110.0
uvicorn==0.29.0
spacy==3.7.4
pydantic==2.6.4
python-dateutil==2.9.0

# MongoDB (data-lake)
pymongo==4.7.2
gridfs==0.4.1

# HTTP & utilities
requests==2.31.0
python-dotenv==1.0.1
jsonschema==4.21.1

# Airflow (installed separately via Docker — listed here for reference)
# apache-airflow==2.9.1

# Testing
pytest==8.1.1
httpx==0.27.0
